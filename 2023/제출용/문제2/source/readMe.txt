실행 명령어
python Q2.py wavlist.txt ../output/Q2.json

필수 패키지 ('pip install -r requirements.txt' 로 설치)
numpy

인자 설명
args1 : wav 파일 경로가 적혀있는 wavlist.txt 파일 경로
args2 : json 출력 경로 (이미 있는 파일이라면 덮어 쓰여지도록 함 / 없으면 기본 구조로 정답을 작성)
